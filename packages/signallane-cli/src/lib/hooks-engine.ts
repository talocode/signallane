export function generateHooks(topic: string) {
  const clean = topic.trim();
  return [
    `I keep seeing ${clean} get framed the wrong way.`,
    `The part people miss about ${clean} is the workflow.`,
    `${clean} is not about more output. It is about better decisions.`,
    `If you use ${clean}, this is the part that matters most.`,
    `Most takes on ${clean} skip the hard part.`,
    `The real test of ${clean} is whether it saves time.`,
    `What surprised me most about ${clean} was the tradeoff.`,
    `There is a better way to think about ${clean}.`,
    `${clean} works when it stays close to the builder’s process.`,
    `You do not need hype to explain ${clean}.`,
  ];
}
